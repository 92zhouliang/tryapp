const getters = {
    // home
    defaultCity: state => state.home.defaultCity,

    // user
    userInfo: state => state.user.userInfo
    // addressList: state => state.user.addressList
}
export default getters